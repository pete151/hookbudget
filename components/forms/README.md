# `components/forms/`

Formulaires réutilisables construits avec **React Hook Form** + **Zod**
(ex. `expense-form.tsx`, `budget-form.tsx`, `login-form.tsx`).

> Sprint 1 : dossier volontairement vide. Les pages Login/Register affichent
> des champs **visuels uniquement** (désactivés) ; aucun formulaire fonctionnel
> n'est encore branché.
