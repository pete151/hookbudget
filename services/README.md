# `services/`

Couche **services** : logique d'accès aux données et appels externes
réutilisables côté serveur (ex. `budget.service.ts`, `ai.service.ts`).

> Sprint 1 : dossier volontairement vide. Aucune logique métier n'est encore
> implémentée. Les services seront ajoutés au fur et à mesure des sprints,
> au-dessus de Prisma (`lib/db/prisma.ts`).
