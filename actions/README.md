# `actions/`

**Server Actions** Next.js (`"use server"`) : mutations appelées directement
depuis les composants (création de dépense, mise à jour de budget, etc.).

> Sprint 1 : dossier volontairement vide. Aucune action n'est encore définie.
> Les Server Actions valideront leurs entrées avec **Zod** et s'appuieront sur
> la couche `services/`.
