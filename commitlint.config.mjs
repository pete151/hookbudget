/**
 * Commitlint — convention Conventional Commits.
 * Exemples : `feat(ai): ajoute le streaming`, `fix(auth): corrige la redirection`.
 */
const config = {
  extends: ["@commitlint/config-conventional"],
};

export default config;
